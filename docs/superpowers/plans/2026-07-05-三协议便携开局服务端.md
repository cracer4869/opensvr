# 三协议便携开局服务端（opensvr）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Go 做一个绿色单 exe，同时提供 FTP(主动+被动)/SFTP/TFTP 服务端，带系统托盘 + 本地 Web 管理页（端口 31944，自动拉起浏览器），供机房给网络设备开局灌固件+配置。

**Architecture:** 单进程、模块隔离。三协议共用一个"根目录"文件系统抽象 `vfs`（= 计数包装 + afero BasePathFs 路径囚笼）。`config` 提供配置读写；`auth` 提供口令；`logbus` 收集事件；`metrics` 采样吞吐速率；`web` 内嵌前端 + REST + SSE；`tray` 托盘常驻并在启动时开浏览器。只有被启用的协议才起监听，未启用零开销。

**Tech Stack:** Go 1.22+；`github.com/fclairamb/ftpserverlib`（FTP，主动+被动）；`github.com/spf13/afero`（文件系统抽象与路径囚笼，ftpserverlib 传递依赖）；`github.com/pkg/sftp` + `golang.org/x/crypto/ssh`（SFTP）；`github.com/pin/tftp`（TFTP）；`github.com/getlantern/systray`（托盘，Windows 纯 syscall 无 cgo）；`gopkg.in/yaml.v3`（配置）；前端原生 HTML/CSS/JS + Canvas，`go:embed` 内嵌。

## Global Constraints

- 目标平台：**Windows amd64**，单静态 exe，无运行时依赖，无 cgo（`CGO_ENABLED=0`）。
- 第一原则：**实用、轻量、低资源占用**。未启用协议零开销；日志与性能采样均用固定容量缓冲。
- 前端**不使用任何框架/图表库**，纯原生 + Canvas。
- Web 管理页绑 `127.0.0.1:31944`，仅本机可访问；服务启动时自动用系统默认浏览器打开。
- 三协议模块统一接口 `Start() error` / `Stop() error` / `Status() Status`，互相独立，一个失败不影响其余。
- 所有路径经 `vfs` 归一化并禁止越出根目录（`../` 囚笼）。
- 提交信息用中文，遵循 TDD：先写失败测试→跑红→最小实现→跑绿→提交。
- 模块目录：`internal/<模块名>`；入口 `cmd/opensvr/main.go`；前端 `internal/web/assets/`。

---

## 文件结构（先锁定分解）

```
go.mod / go.sum
cmd/opensvr/main.go            装配所有模块、systray 主循环、启动时开浏览器
internal/config/config.go      Config 结构、默认值、Load/Save(yaml)
internal/config/config_test.go
internal/vfs/vfs.go            afero BasePathFs 囚笼 + 计数包装；给三协议用
internal/vfs/vfs_test.go       路径穿越/越界、字节计数
internal/logbus/logbus.go      事件环形缓冲 + 订阅(SSE)
internal/logbus/logbus_test.go
internal/metrics/metrics.go    上/下行原子计数、每秒采样、滚动窗口、速率
internal/metrics/metrics_test.go
internal/auth/auth.go          用户库、Authenticate、匿名开关
internal/auth/auth_test.go
internal/netinfo/netinfo.go    枚举网卡 IP/子网
internal/netinfo/netinfo_test.go
internal/ftpsrv/ftpsrv.go      封装 ftpserverlib，主动+被动，driver 接 vfs+auth+logbus
internal/ftpsrv/ftpsrv_test.go 端到端：被动/主动各传一次
internal/sftpsrv/sftpsrv.go    ssh 服务 + sftp RequestServer，hostkey 持久化，接 vfs+auth
internal/sftpsrv/sftpsrv_test.go
internal/tftpsrv/tftpsrv.go    pin/tftp RRQ/WRQ，接 vfs
internal/tftpsrv/tftpsrv_test.go
internal/hostkey/hostkey.go    SFTP 主机密钥生成/加载(ed25519)
internal/hostkey/hostkey_test.go
internal/server/manager.go     Manager：持三协议实例 + 统一启停/状态
internal/server/manager_test.go
internal/web/web.go            HTTP：静态、REST、SSE(日志+速率)
internal/web/web_test.go
internal/web/browser.go        系统默认浏览器打开 URL（Windows）
internal/web/assets/index.html 管理页（含 Canvas 曲线）
internal/web/assets/app.js
internal/web/assets/style.css
docs/USAGE.md                  使用说明 + 华为/H3C/思科开局命令示例
```

各模块契约（供其它任务引用的精确签名）：

- `config`：
  - `type Config struct { RootDir string; FTP ProtoCfg; SFTP ProtoCfg; TFTP ProtoCfg; Web WebCfg; Auth AuthCfg; PassiveRange [2]int; AutoOpenBrowser bool; LogToFile bool }`
  - `type ProtoCfg struct { Enabled bool; Port int }`
  - `type WebCfg struct { Port int }`（默认 31944）
  - `type AuthCfg struct { User, Pass string; Anonymous bool }`
  - `func Default() *Config`；`func Load(path string) (*Config, error)`；`func (c *Config) Save(path string) error`
- `vfs`：`func New(root string) (*VFS, error)`；`func (v *VFS) Fs() afero.Fs`；`func (v *VFS) SetRoot(root string) error`；内部读写经 `metrics` 计数（通过注入的计数回调）。
- `metrics`：`func AddUp(n int64)` / `func AddDown(n int64)`（全局原子）；`func Snapshot() Sample`；`type Sample struct { UpBps, DownBps float64; UpTotal, DownTotal int64 }`；`func History() []Sample`；`func Start(stop <-chan struct{})`（每秒采样）。
- `auth`：`type Store struct{...}`；`func New(cfg config.AuthCfg) *Store`；`func (s *Store) Authenticate(user, pass string) (ok bool, anon bool)`；`func (s *Store) Update(cfg config.AuthCfg)`。
- `logbus`：`type Event struct { Time string; Proto, User, Action, Path string; OK bool; Msg string }`；`func Emit(Event)`；`func Recent() []Event`；`func Subscribe() (<-chan Event, func())`。
- `netinfo`：`type NIC struct { Name, IP, CIDR string }`；`func List() []NIC`。
- 协议模块统一：`type Status struct { Running bool; Port int; Err string }`；各 `func New(...) *XxxServer`；`Start() error`；`Stop() error`；`Status() Status`。
- `hostkey`：`func LoadOrCreate(path string) (ssh.Signer, error)`。
- `server.Manager`：`func New(cfg *config.Config, ...) *Manager`；`StartFTP/StopFTP/...`；`SetRoot(string)`；`Statuses() map[string]Status`。

---

## Task 0: 环境准备（Go 工具链 + 模块初始化）

**Files:**
- Create: `go.mod`

- [ ] **Step 1: 安装 Go（本机当前无 go 命令）**

在会话中执行（PowerShell/winget）：
```
! winget install --id GoLang.Go -e --accept-source-agreements --accept-package-agreements
```
或用户自行从 https://go.dev/dl/ 装。装完新开 shell 验证：
Run: `go version`
Expected: 输出 `go version go1.22...` 或更高。

- [ ] **Step 2: 初始化模块**

Run:
```bash
cd "D:/01.System/03.Genius_Dev/03.Claude/02.ftp_sftp开发"
go mod init opensvr
```
Expected: 生成 `go.mod`，内容含 `module opensvr` 与 `go 1.22`。

- [ ] **Step 3: 拉取依赖**

Run:
```bash
go get github.com/fclairamb/ftpserverlib@latest github.com/spf13/afero@latest github.com/pkg/sftp@latest golang.org/x/crypto/ssh@latest github.com/pin/tftp@latest github.com/getlantern/systray@latest gopkg.in/yaml.v3@latest
```
Expected: `go.mod` 出现上述 require；无报错。

- [ ] **Step 4: 提交**
```bash
git add go.mod go.sum && git commit -m "chore: 初始化 Go 模块与依赖"
```

---

## Task 1: config 模块

**Files:**
- Create: `internal/config/config.go`, `internal/config/config_test.go`

**Interfaces:**
- Produces: `Config`, `ProtoCfg`, `WebCfg`, `AuthCfg`（见文件结构契约）；`Default()`, `Load(path)`, `(*Config).Save(path)`。

- [ ] **Step 1: 写失败测试**
```go
package config

import ("os"; "path/filepath"; "testing")

func TestDefault(t *testing.T) {
    c := Default()
    if c.Web.Port != 31944 { t.Fatalf("web port = %d, want 31944", c.Web.Port) }
    if c.FTP.Port != 21 || c.SFTP.Port != 22 || c.TFTP.Port != 69 {
        t.Fatalf("proto ports wrong: %+v", c)
    }
    if c.Auth.User != "admin" || c.Auth.Pass != "admin" { t.Fatal("default auth wrong") }
    if c.PassiveRange != [2]int{50000, 50100} { t.Fatal("passive range wrong") }
    if !c.AutoOpenBrowser { t.Fatal("AutoOpenBrowser should default true") }
}

func TestSaveLoadRoundTrip(t *testing.T) {
    p := filepath.Join(t.TempDir(), "config.yaml")
    c := Default(); c.FTP.Port = 2121; c.RootDir = "X:/fw"
    if err := c.Save(p); err != nil { t.Fatal(err) }
    got, err := Load(p)
    if err != nil { t.Fatal(err) }
    if got.FTP.Port != 2121 || got.RootDir != "X:/fw" { t.Fatalf("roundtrip mismatch: %+v", got) }
}

func TestLoadMissingReturnsDefault(t *testing.T) {
    got, err := Load(filepath.Join(t.TempDir(), "nope.yaml"))
    if err != nil { t.Fatal(err) }
    if got.Web.Port != 31944 { t.Fatal("missing file should yield defaults") }
    _ = os.Remove
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/config/` → FAIL（未定义）。

- [ ] **Step 3: 实现**
```go
package config

import ("errors"; "os"; "gopkg.in/yaml.v3")

type ProtoCfg struct { Enabled bool `yaml:"enabled"`; Port int `yaml:"port"` }
type WebCfg struct { Port int `yaml:"port"` }
type AuthCfg struct { User string `yaml:"user"`; Pass string `yaml:"pass"`; Anonymous bool `yaml:"anonymous"` }

type Config struct {
    RootDir string `yaml:"root_dir"`
    FTP ProtoCfg `yaml:"ftp"`
    SFTP ProtoCfg `yaml:"sftp"`
    TFTP ProtoCfg `yaml:"tftp"`
    Web WebCfg `yaml:"web"`
    Auth AuthCfg `yaml:"auth"`
    PassiveRange [2]int `yaml:"passive_range"`
    AutoOpenBrowser bool `yaml:"auto_open_browser"`
    LogToFile bool `yaml:"log_to_file"`
}

func Default() *Config {
    return &Config{
        RootDir: "./data",
        FTP: ProtoCfg{Enabled: false, Port: 21},
        SFTP: ProtoCfg{Enabled: false, Port: 22},
        TFTP: ProtoCfg{Enabled: false, Port: 69},
        Web: WebCfg{Port: 31944},
        Auth: AuthCfg{User: "admin", Pass: "admin", Anonymous: false},
        PassiveRange: [2]int{50000, 50100},
        AutoOpenBrowser: true,
        LogToFile: false,
    }
}

func Load(path string) (*Config, error) {
    b, err := os.ReadFile(path)
    if errors.Is(err, os.ErrNotExist) { return Default(), nil }
    if err != nil { return nil, err }
    c := Default()
    if err := yaml.Unmarshal(b, c); err != nil { return nil, err }
    return c, nil
}

func (c *Config) Save(path string) error {
    b, err := yaml.Marshal(c)
    if err != nil { return err }
    return os.WriteFile(path, b, 0644)
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/config/` → PASS。
- [ ] **Step 5: 提交** — `git add internal/config && git commit -m "feat(config): 配置结构与默认值/读写"`

---

## Task 2: metrics 模块（吞吐采样，供性能曲线）

**Files:**
- Create: `internal/metrics/metrics.go`, `internal/metrics/metrics_test.go`

**Interfaces:**
- Produces: `AddUp(int64)`, `AddDown(int64)`, `Snapshot() Sample`, `History() []Sample`, `Start(stop <-chan struct{})`；`type Sample struct { UpBps, DownBps float64; UpTotal, DownTotal int64 }`。
- Consumes: 无。被 `vfs` 计数回调与 `web` 使用。

- [ ] **Step 1: 写失败测试**
```go
package metrics

import ("testing")

func TestCountersAndSample(t *testing.T) {
    Reset()
    AddUp(1000); AddDown(500)
    s := Snapshot()
    if s.UpTotal != 1000 || s.DownTotal != 500 { t.Fatalf("totals wrong: %+v", s) }
}

func TestSampleRateComputation(t *testing.T) {
    Reset()
    AddUp(2000)
    // 手动推进一个采样周期（1s 视为 dt=1）
    sampleOnce(1.0)
    h := History()
    if len(h) == 0 { t.Fatal("history empty") }
    if h[len(h)-1].UpBps != 2000 { t.Fatalf("up bps = %v, want 2000", h[len(h)-1].UpBps) }
}

func TestHistoryCapped(t *testing.T) {
    Reset()
    for i := 0; i < 200; i++ { sampleOnce(1.0) }
    if len(History()) > maxPoints { t.Fatalf("history not capped: %d", len(History())) }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/metrics/` → FAIL。

- [ ] **Step 3: 实现**
```go
package metrics

import ("sync"; "sync/atomic"; "time")

const maxPoints = 60

type Sample struct { UpBps, DownBps float64; UpTotal, DownTotal int64 }

var (
    upTotal, downTotal int64
    lastUp, lastDown   int64
    mu   sync.Mutex
    hist []Sample
)

func AddUp(n int64)   { atomic.AddInt64(&upTotal, n) }
func AddDown(n int64) { atomic.AddInt64(&downTotal, n) }

func Snapshot() Sample {
    mu.Lock(); defer mu.Unlock()
    s := Sample{UpTotal: atomic.LoadInt64(&upTotal), DownTotal: atomic.LoadInt64(&downTotal)}
    if len(hist) > 0 { s.UpBps = hist[len(hist)-1].UpBps; s.DownBps = hist[len(hist)-1].DownBps }
    return s
}

func History() []Sample {
    mu.Lock(); defer mu.Unlock()
    out := make([]Sample, len(hist)); copy(out, hist); return out
}

// sampleOnce 用 dt 秒计算这一周期速率并入队（测试可注入 dt）。
func sampleOnce(dt float64) {
    u := atomic.LoadInt64(&upTotal); d := atomic.LoadInt64(&downTotal)
    mu.Lock()
    s := Sample{
        UpBps: float64(u-lastUp) / dt, DownBps: float64(d-lastDown) / dt,
        UpTotal: u, DownTotal: d,
    }
    lastUp, lastDown = u, d
    hist = append(hist, s)
    if len(hist) > maxPoints { hist = hist[len(hist)-maxPoints:] }
    mu.Unlock()
}

func Reset() {
    mu.Lock(); defer mu.Unlock()
    atomic.StoreInt64(&upTotal, 0); atomic.StoreInt64(&downTotal, 0)
    lastUp, lastDown = 0, 0; hist = nil
}

func Start(stop <-chan struct{}) {
    t := time.NewTicker(time.Second); defer t.Stop()
    for {
        select {
        case <-stop: return
        case <-t.C: sampleOnce(1.0)
        }
    }
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/metrics/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(metrics): 上下行吞吐采样与滚动窗口"`

---

## Task 3: vfs 模块（路径囚笼 + 字节计数）

**Files:**
- Create: `internal/vfs/vfs.go`, `internal/vfs/vfs_test.go`

**Interfaces:**
- Consumes: `metrics.AddUp/AddDown`。
- Produces: `func New(root string) (*VFS, error)`；`func (v *VFS) Fs() afero.Fs`；`func (v *VFS) SetRoot(root string) error`；`func (v *VFS) Root() string`。`Fs()` 返回的 afero.Fs 已囚笼在 root 内，且读写自动计入 metrics。

**说明:** 用 `afero.NewBasePathFs(afero.NewOsFs(), root)` 天然把所有路径囚在 root 内（越界返回错误）。再包一层 `countingFs`，在 `Open/OpenFile/Create` 返回的 File 的 Read/Write 上累加 metrics。

- [ ] **Step 1: 写失败测试**
```go
package vfs

import ("os"; "path/filepath"; "testing"; "opensvr/internal/metrics")

func TestJailBlocksTraversal(t *testing.T) {
    root := t.TempDir()
    v, err := New(root); if err != nil { t.Fatal(err) }
    // 试图越界读取上级文件应失败
    if _, err := v.Fs().Open("../secret.txt"); err == nil {
        t.Fatal("traversal should be blocked")
    }
}

func TestWriteReadCounts(t *testing.T) {
    metrics.Reset()
    root := t.TempDir()
    v, _ := New(root)
    f, err := v.Fs().Create("a.bin"); if err != nil { t.Fatal(err) }
    f.Write([]byte("hello")); f.Close()
    if metrics.Snapshot().UpTotal != 5 { t.Fatalf("up total = %d, want 5", metrics.Snapshot().UpTotal) }

    rf, _ := v.Fs().Open("a.bin")
    buf := make([]byte, 5); rf.Read(buf); rf.Close()
    if metrics.Snapshot().DownTotal != 5 { t.Fatalf("down total = %d, want 5", metrics.Snapshot().DownTotal) }
    _ = os.WriteFile; _ = filepath.Join
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/vfs/` → FAIL。

- [ ] **Step 3: 实现**
```go
package vfs

import ("os"; "sync"; "github.com/spf13/afero"; "opensvr/internal/metrics")

type VFS struct { mu sync.RWMutex; root string; base afero.Fs }

func New(root string) (*VFS, error) {
    if err := os.MkdirAll(root, 0755); err != nil { return nil, err }
    v := &VFS{root: root}
    v.base = &countingFs{Fs: afero.NewBasePathFs(afero.NewOsFs(), root)}
    return v, nil
}

func (v *VFS) Fs() afero.Fs { v.mu.RLock(); defer v.mu.RUnlock(); return v.base }
func (v *VFS) Root() string { v.mu.RLock(); defer v.mu.RUnlock(); return v.root }

func (v *VFS) SetRoot(root string) error {
    if err := os.MkdirAll(root, 0755); err != nil { return err }
    v.mu.Lock(); defer v.mu.Unlock()
    v.root = root
    v.base = &countingFs{Fs: afero.NewBasePathFs(afero.NewOsFs(), root)}
    return nil
}

// countingFs 包装 afero.Fs，对返回的文件读写计入 metrics。
type countingFs struct{ afero.Fs }

func (c *countingFs) Open(name string) (afero.File, error) {
    f, err := c.Fs.Open(name); if err != nil { return nil, err }; return &countingFile{File: f}, nil
}
func (c *countingFs) OpenFile(name string, flag int, perm os.FileMode) (afero.File, error) {
    f, err := c.Fs.OpenFile(name, flag, perm); if err != nil { return nil, err }; return &countingFile{File: f}, nil
}
func (c *countingFs) Create(name string) (afero.File, error) {
    f, err := c.Fs.Create(name); if err != nil { return nil, err }; return &countingFile{File: f}, nil
}

type countingFile struct{ afero.File }

func (f *countingFile) Read(p []byte) (int, error) {
    n, err := f.File.Read(p); if n > 0 { metrics.AddDown(int64(n)) }; return n, err
}
func (f *countingFile) Write(p []byte) (int, error) {
    n, err := f.File.Write(p); if n > 0 { metrics.AddUp(int64(n)) }; return n, err
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/vfs/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(vfs): 路径囚笼与读写字节计数"`

---

## Task 4: auth 模块

**Files:**
- Create: `internal/auth/auth.go`, `internal/auth/auth_test.go`

**Interfaces:**
- Consumes: `config.AuthCfg`。
- Produces: `func New(cfg config.AuthCfg) *Store`；`func (s *Store) Authenticate(user, pass string) (ok, anon bool)`；`func (s *Store) Update(cfg config.AuthCfg)`；`func (s *Store) Creds() (user, pass string, anon bool)`（供 Web 明文展示）。

- [ ] **Step 1: 写失败测试**
```go
package auth

import ("testing"; "opensvr/internal/config")

func TestAuthenticate(t *testing.T) {
    s := New(config.AuthCfg{User: "admin", Pass: "admin", Anonymous: false})
    if ok, _ := s.Authenticate("admin", "admin"); !ok { t.Fatal("valid creds rejected") }
    if ok, _ := s.Authenticate("admin", "wrong"); ok { t.Fatal("bad pass accepted") }
    if ok, _ := s.Authenticate("anonymous", "x"); ok { t.Fatal("anon should be off") }
}

func TestAnonymous(t *testing.T) {
    s := New(config.AuthCfg{User: "admin", Pass: "admin", Anonymous: true})
    ok, anon := s.Authenticate("anonymous", "whatever")
    if !ok || !anon { t.Fatal("anonymous login should pass when enabled") }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/auth/` → FAIL。

- [ ] **Step 3: 实现**
```go
package auth

import ("crypto/subtle"; "sync"; "opensvr/internal/config")

type Store struct { mu sync.RWMutex; cfg config.AuthCfg }

func New(cfg config.AuthCfg) *Store { return &Store{cfg: cfg} }

func (s *Store) Update(cfg config.AuthCfg) { s.mu.Lock(); s.cfg = cfg; s.mu.Unlock() }

func (s *Store) Creds() (string, string, bool) {
    s.mu.RLock(); defer s.mu.RUnlock(); return s.cfg.User, s.cfg.Pass, s.cfg.Anonymous
}

func (s *Store) Authenticate(user, pass string) (bool, bool) {
    s.mu.RLock(); defer s.mu.RUnlock()
    if s.cfg.Anonymous && (user == "anonymous" || user == "ftp") { return true, true }
    u := subtle.ConstantTimeCompare([]byte(user), []byte(s.cfg.User))
    p := subtle.ConstantTimeCompare([]byte(pass), []byte(s.cfg.Pass))
    return u == 1 && p == 1, false
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/auth/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(auth): 口令校验与匿名开关"`

---

## Task 5: logbus 模块

**Files:**
- Create: `internal/logbus/logbus.go`, `internal/logbus/logbus_test.go`

**Interfaces:**
- Produces: `type Event struct { Time, Proto, User, Action, Path string; OK bool; Msg string }`；`Emit(Event)`；`Recent() []Event`；`Subscribe() (<-chan Event, func())`。

- [ ] **Step 1: 写失败测试**
```go
package logbus

import ("testing")

func TestEmitAndRecent(t *testing.T) {
    Reset()
    Emit(Event{Proto: "ftp", Action: "upload", OK: true})
    if len(Recent()) != 1 { t.Fatalf("recent len = %d", len(Recent())) }
}

func TestSubscribeReceives(t *testing.T) {
    Reset()
    ch, cancel := Subscribe(); defer cancel()
    Emit(Event{Proto: "sftp", Action: "download", OK: true})
    select {
    case e := <-ch:
        if e.Proto != "sftp" { t.Fatalf("got %+v", e) }
    default:
        t.Fatal("subscriber did not receive event")
    }
}

func TestRingCapped(t *testing.T) {
    Reset()
    for i := 0; i < maxEvents+50; i++ { Emit(Event{Proto: "tftp"}) }
    if len(Recent()) > maxEvents { t.Fatalf("ring not capped: %d", len(Recent())) }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/logbus/` → FAIL。

- [ ] **Step 3: 实现**
```go
package logbus

import ("sync"; "time")

const maxEvents = 500

type Event struct {
    Time string `json:"time"`; Proto string `json:"proto"`; User string `json:"user"`
    Action string `json:"action"`; Path string `json:"path"`; OK bool `json:"ok"`; Msg string `json:"msg"`
}

var (
    mu   sync.RWMutex
    ring []Event
    subs = map[int]chan Event{}
    seq  int
)

func Emit(e Event) {
    if e.Time == "" { e.Time = time.Now().Format("15:04:05") }
    mu.Lock()
    ring = append(ring, e)
    if len(ring) > maxEvents { ring = ring[len(ring)-maxEvents:] }
    for _, ch := range subs { select { case ch <- e: default: } }
    mu.Unlock()
}

func Recent() []Event {
    mu.RLock(); defer mu.RUnlock()
    out := make([]Event, len(ring)); copy(out, ring); return out
}

func Subscribe() (<-chan Event, func()) {
    ch := make(chan Event, 64)
    mu.Lock(); id := seq; seq++; subs[id] = ch; mu.Unlock()
    return ch, func() { mu.Lock(); delete(subs, id); close(ch); mu.Unlock() }
}

func Reset() { mu.Lock(); ring = nil; subs = map[int]chan Event{}; seq = 0; mu.Unlock() }
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/logbus/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(logbus): 事件环形缓冲与订阅"`

---

## Task 6: netinfo 模块

**Files:**
- Create: `internal/netinfo/netinfo.go`, `internal/netinfo/netinfo_test.go`

**Interfaces:**
- Produces: `type NIC struct { Name, IP, CIDR string }`；`func List() []NIC`（仅列出 up 且非回环的 IPv4）。

- [ ] **Step 1: 写失败测试**
```go
package netinfo

import ("testing")

func TestListReturnsSlice(t *testing.T) {
    // 不假设具体网卡，只验证不 panic 且字段合法
    for _, n := range List() {
        if n.IP == "" || n.CIDR == "" { t.Fatalf("bad nic: %+v", n) }
    }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/netinfo/` → FAIL（未定义）。

- [ ] **Step 3: 实现**
```go
package netinfo

import ("net")

type NIC struct { Name, IP, CIDR string }

func List() []NIC {
    var out []NIC
    ifaces, err := net.Interfaces(); if err != nil { return out }
    for _, ifc := range ifaces {
        if ifc.Flags&net.FlagUp == 0 || ifc.Flags&net.FlagLoopback != 0 { continue }
        addrs, _ := ifc.Addrs()
        for _, a := range addrs {
            ipn, ok := a.(*net.IPNet); if !ok { continue }
            ip4 := ipn.IP.To4(); if ip4 == nil { continue }
            out = append(out, NIC{Name: ifc.Name, IP: ip4.String(), CIDR: ipn.String()})
        }
    }
    return out
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/netinfo/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(netinfo): 枚举本机网卡IP与子网"`

---

## Task 7: hostkey 模块（SFTP 主机密钥）

**Files:**
- Create: `internal/hostkey/hostkey.go`, `internal/hostkey/hostkey_test.go`

**Interfaces:**
- Produces: `func LoadOrCreate(path string) (ssh.Signer, error)`（ed25519，PEM 持久化，缺失则生成）。

- [ ] **Step 1: 写失败测试**
```go
package hostkey

import ("path/filepath"; "testing")

func TestLoadOrCreatePersists(t *testing.T) {
    p := filepath.Join(t.TempDir(), "hostkey")
    s1, err := LoadOrCreate(p); if err != nil { t.Fatal(err) }
    s2, err := LoadOrCreate(p); if err != nil { t.Fatal(err) }
    if string(s1.PublicKey().Marshal()) != string(s2.PublicKey().Marshal()) {
        t.Fatal("host key not persisted (changed between loads)")
    }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/hostkey/` → FAIL。

- [ ] **Step 3: 实现**
```go
package hostkey

import (
    "crypto/ed25519"; "crypto/rand"; "encoding/pem"; "errors"; "os"
    "golang.org/x/crypto/ssh"
)

func LoadOrCreate(path string) (ssh.Signer, error) {
    b, err := os.ReadFile(path)
    if err == nil { return ssh.ParsePrivateKey(b) }
    if !errors.Is(err, os.ErrNotExist) { return nil, err }
    _, priv, err := ed25519.GenerateKey(rand.Reader); if err != nil { return nil, err }
    pemBlock, err := ssh.MarshalPrivateKey(priv, ""); if err != nil { return nil, err }
    if err := os.WriteFile(path, pem.EncodeToMemory(pemBlock), 0600); err != nil { return nil, err }
    return ssh.NewSignerFromSigner(priv)
}
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/hostkey/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(hostkey): SFTP主机密钥生成与持久化"`

---

## Task 8: ftpsrv 模块（FTP，主动+被动）

**Files:**
- Create: `internal/ftpsrv/ftpsrv.go`, `internal/ftpsrv/ftpsrv_test.go`

**Interfaces:**
- Consumes: `vfs.VFS`, `auth.Store`, `config.Config`（端口/被动段）, `logbus`。
- Produces: `func New(v *vfs.VFS, a *auth.Store, port int, passive [2]int) *Server`；`Start() error`；`Stop() error`；`Status() server.Status`（用本地 `Status` 结构，见 server 包；此处先各自定义 `type Status struct{Running bool; Port int; Err string}` 并在 Task 11 统一）。

**说明:** 实现 ftpserverlib `MainDriver`。`AuthUser` 返回 `ClientDriver`（即 `afero.Fs`）用 `v.Fs()`。`GetSettings` 里设 `ListenAddr`、`PassiveTransferPortRange`；主动模式默认启用，无需额外设置。

- [ ] **Step 1: 写失败的端到端测试（被动 + 主动各一次）**
```go
package ftpsrv

import (
    "bytes"; "testing"; "time"
    "github.com/jlaffaye/ftp"
    "opensvr/internal/auth"; "opensvr/internal/config"; "opensvr/internal/vfs"
)

func startTestServer(t *testing.T) (*Server, string) {
    t.Helper()
    v, _ := vfs.New(t.TempDir())
    a := auth.New(config.AuthCfg{User: "admin", Pass: "admin"})
    s := New(v, a, 0, [2]int{0, 0}) // 0=随机端口；被动段 0 让库自选
    if err := s.Start(); err != nil { t.Fatal(err) }
    time.Sleep(100 * time.Millisecond)
    return s, s.Addr()
}

func TestFTPPassiveUploadDownload(t *testing.T) {
    s, addr := startTestServer(t); defer s.Stop()
    c, err := ftp.Dial(addr, ftp.DialWithTimeout(3*time.Second)); if err != nil { t.Fatal(err) }
    if err := c.Login("admin", "admin"); err != nil { t.Fatal(err) }
    data := []byte("firmware-bytes")
    if err := c.Stor("fw.bin", bytes.NewReader(data)); err != nil { t.Fatal(err) }
    r, err := c.Retr("fw.bin"); if err != nil { t.Fatal(err) }
    got := new(bytes.Buffer); got.ReadFrom(r); r.Close()
    if !bytes.Equal(got.Bytes(), data) { t.Fatalf("roundtrip mismatch") }
}
```
（主动模式测试：另加 `TestFTPActiveMode`，用 `ftp.DialWithDisabledEPSV(true)` 触发 PORT/主动，断言同样能上传下载。需 `go get github.com/jlaffaye/ftp` 作为测试依赖。）

- [ ] **Step 2: 跑红** — Run: `go test ./internal/ftpsrv/` → FAIL。

- [ ] **Step 3: 实现**（driver + Server 封装）
```go
package ftpsrv

import (
    "crypto/tls"; "fmt"; "sync"
    ftpserver "github.com/fclairamb/ftpserverlib"
    "github.com/spf13/afero"
    "opensvr/internal/auth"; "opensvr/internal/logbus"; "opensvr/internal/vfs"
)

type Status struct { Running bool; Port int; Err string }

type Server struct {
    v *vfs.VFS; a *auth.Store; port int; passive [2]int
    mu sync.Mutex; srv *ftpserver.FtpServer; running bool; err string; boundPort int
}

func New(v *vfs.VFS, a *auth.Store, port int, passive [2]int) *Server {
    return &Server{v: v, a: a, port: port, passive: passive}
}

// ----- MainDriver 实现 -----
type driver struct{ s *Server }

func (d *driver) GetSettings() (*ftpserver.Settings, error) {
    st := &ftpserver.Settings{ ListenAddr: fmt.Sprintf("0.0.0.0:%d", d.s.port) }
    if d.s.passive[1] > 0 {
        st.PassiveTransferPortRange = &ftpserver.PortRange{Start: d.s.passive[0], End: d.s.passive[1]}
    }
    return st, nil
}
func (d *driver) ClientConnected(cc ftpserver.ClientContext) (string, error) { return "opensvr", nil }
func (d *driver) ClientDisconnected(cc ftpserver.ClientContext) {}
func (d *driver) AuthUser(cc ftpserver.ClientContext, user, pass string) (ftpserver.ClientDriver, error) {
    ok, _ := d.s.a.Authenticate(user, pass)
    if !ok {
        logbus.Emit(logbus.Event{Proto: "ftp", User: user, Action: "login", OK: false, Msg: "auth failed"})
        return nil, fmt.Errorf("authentication failed")
    }
    logbus.Emit(logbus.Event{Proto: "ftp", User: user, Action: "login", OK: true})
    return d.s.v.Fs().(afero.Fs), nil
}
func (d *driver) GetTLSConfig() (*tls.Config, error) { return nil, fmt.Errorf("TLS not enabled") }

func (s *Server) Start() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if s.running { return nil }
    s.srv = ftpserver.NewFtpServer(&driver{s: s})
    if err := s.srv.Listen(); err != nil { s.err = err.Error(); return err }
    s.boundPort = resolvePort(s.srv, s.port)
    s.running = true; s.err = ""
    go s.srv.Serve()
    return nil
}

func (s *Server) Stop() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if !s.running { return nil }
    _ = s.srv.Stop(); s.running = false; return nil
}

func (s *Server) Status() Status {
    s.mu.Lock(); defer s.mu.Unlock()
    return Status{Running: s.running, Port: s.boundPort, Err: s.err}
}
func (s *Server) Addr() string { return fmt.Sprintf("127.0.0.1:%d", s.Status().Port) }
```
> 注：`resolvePort` 从 `s.srv.Listener().Addr()` 解析实际端口（支持 0=随机，便于测试）。实现时查 ftpserverlib 当前版本获取监听地址的方法（`ListenerAddr` 或 `Addr()`），据此写 `resolvePort`；若该版本无导出方法，则测试改用固定高端口（如 2121）。

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/ftpsrv/` → PASS（被动+主动）。
- [ ] **Step 5: 提交** — `git commit -am "feat(ftpsrv): FTP服务端(主动+被动)封装"`

---

## Task 9: sftpsrv 模块

**Files:**
- Create: `internal/sftpsrv/sftpsrv.go`, `internal/sftpsrv/sftpsrv_test.go`

**Interfaces:**
- Consumes: `vfs.VFS`, `auth.Store`, `hostkey.LoadOrCreate`, `logbus`。
- Produces: `func New(v *vfs.VFS, a *auth.Store, signer ssh.Signer, port int) *Server`；`Start()/Stop()/Status()`；`Addr()`。

**说明:** 用 `ssh.NewServerConn` 接连接，认证走 `PasswordCallback`→`auth.Authenticate`；对 `session` 通道的 `subsystem sftp` 请求，用 `sftp.NewServer(channel)`。为把根目录囚在 vfs.root，使用 `sftp.NewServer` 的 `WithServerWorkingDirectory` 不足以囚笼；**改用 `sftp.Handlers` + `sftp.NewRequestServer`**，Handlers 的 FileGet/FilePut/FileList/FileCmd 全部走 `v.Fs()`（afero.Fs，已囚笼+计数）。

- [ ] **Step 1: 写失败的端到端测试**
```go
package sftpsrv

import (
    "bytes"; "io"; "testing"; "time"
    "github.com/pkg/sftp"; "golang.org/x/crypto/ssh"
    "opensvr/internal/auth"; "opensvr/internal/config"; "opensvr/internal/hostkey"; "opensvr/internal/vfs"
)

func TestSFTPUploadDownload(t *testing.T) {
    v, _ := vfs.New(t.TempDir())
    a := auth.New(config.AuthCfg{User: "admin", Pass: "admin"})
    signer, _ := hostkey.LoadOrCreate(t.TempDir() + "/hk")
    s := New(v, a, signer, 0); if err := s.Start(); err != nil { t.Fatal(err) }
    defer s.Stop(); time.Sleep(100 * time.Millisecond)

    cfg := &ssh.ClientConfig{User: "admin", Auth: []ssh.AuthMethod{ssh.Password("admin")}, HostKeyCallback: ssh.InsecureIgnoreHostKey()}
    conn, err := ssh.Dial("tcp", s.Addr(), cfg); if err != nil { t.Fatal(err) }
    cl, err := sftp.NewClient(conn); if err != nil { t.Fatal(err) }
    defer cl.Close()

    w, _ := cl.Create("cfg.txt"); w.Write([]byte("device-config")); w.Close()
    r, _ := cl.Open("cfg.txt"); got, _ := io.ReadAll(r); r.Close()
    if !bytes.Equal(got, []byte("device-config")) { t.Fatal("sftp roundtrip mismatch") }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/sftpsrv/` → FAIL。

- [ ] **Step 3: 实现**（ssh 监听 + RequestServer over afero）
```go
package sftpsrv

import (
    "fmt"; "net"; "sync"
    "github.com/pkg/sftp"; "golang.org/x/crypto/ssh"
    "opensvr/internal/auth"; "opensvr/internal/logbus"; "opensvr/internal/vfs"
)

type Status struct { Running bool; Port int; Err string }

type Server struct {
    v *vfs.VFS; a *auth.Store; signer ssh.Signer; port int
    mu sync.Mutex; ln net.Listener; running bool; err string; boundPort int
}

func New(v *vfs.VFS, a *auth.Store, signer ssh.Signer, port int) *Server {
    return &Server{v: v, a: a, signer: signer, port: port}
}

func (s *Server) Start() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if s.running { return nil }
    sc := &ssh.ServerConfig{
        PasswordCallback: func(c ssh.ConnMetadata, pass []byte) (*ssh.Permissions, error) {
            ok, _ := s.a.Authenticate(c.User(), string(pass))
            if !ok {
                logbus.Emit(logbus.Event{Proto: "sftp", User: c.User(), Action: "login", OK: false})
                return nil, fmt.Errorf("auth failed")
            }
            logbus.Emit(logbus.Event{Proto: "sftp", User: c.User(), Action: "login", OK: true})
            return &ssh.Permissions{}, nil
        },
    }
    sc.AddHostKey(s.signer)
    ln, err := net.Listen("tcp", fmt.Sprintf("0.0.0.0:%d", s.port))
    if err != nil { s.err = err.Error(); return err }
    s.ln = ln; s.boundPort = ln.Addr().(*net.TCPAddr).Port
    s.running = true; s.err = ""
    go s.acceptLoop(sc)
    return nil
}

func (s *Server) acceptLoop(sc *ssh.ServerConfig) {
    for {
        c, err := s.ln.Accept(); if err != nil { return }
        go s.handleConn(c, sc)
    }
}

func (s *Server) handleConn(c net.Conn, sc *ssh.ServerConfig) {
    conn, chans, reqs, err := ssh.NewServerConn(c, sc); if err != nil { c.Close(); return }
    defer conn.Close()
    go ssh.DiscardRequests(reqs)
    for nc := range chans {
        if nc.ChannelType() != "session" { nc.Reject(ssh.UnknownChannelType, "only session"); continue }
        ch, requests, err := nc.Accept(); if err != nil { continue }
        go func(in <-chan *ssh.Request) {
            for r := range in { r.Reply(r.Type == "subsystem", nil) }
        }(requests)
        // Handlers 走 vfs.Fs()（afero，已囚笼+计数）
        h := newHandlers(s.v)
        srv := sftp.NewRequestServer(ch, h)
        go func() { _ = srv.Serve(); ch.Close() }()
    }
}

func (s *Server) Stop() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if !s.running { return nil }
    _ = s.ln.Close(); s.running = false; return nil
}
func (s *Server) Status() Status { s.mu.Lock(); defer s.mu.Unlock(); return Status{s.running, s.boundPort, s.err} }
func (s *Server) Addr() string { return fmt.Sprintf("127.0.0.1:%d", s.Status().Port) }
```
外加 `handlers.go`：`newHandlers(v *vfs.VFS) sftp.Handlers`，用 `sftp.Handlers{FileGet, FilePut, FileList, FileCmd}` 适配 `afero.Fs`（Open/Create/OpenFile/Stat/Readdir/Remove/Mkdir/Rename）。实现时逐个方法把 `sftp.Request.Filepath` 交给 `v.Fs()`。
> 若适配 afero→sftp.Handlers 代码量偏大，可退化用 `sftp.NewServer(ch, sftp.WithServerWorkingDirectory(v.Root()))`：此法直接用 OS 文件系统但把工作目录设为 root。**注意**：该法不提供强囚笼，SFTP 客户端仍可用绝对路径越界，安全性弱于 afero 方案。**首选 afero Handlers 方案**；仅当当前 pkg/sftp 版本 API 受限时才退化，并在 logbus/文档中标注该限制。

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/sftpsrv/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(sftpsrv): SFTP服务端(ssh+囚笼文件系统)"`

---

## Task 10: tftpsrv 模块

**Files:**
- Create: `internal/tftpsrv/tftpsrv.go`, `internal/tftpsrv/tftpsrv_test.go`

**Interfaces:**
- Consumes: `vfs.VFS`, `logbus`。
- Produces: `func New(v *vfs.VFS, port int) *Server`；`Start()/Stop()/Status()`；`Addr()`。

**说明:** `tftp.NewServer(readHandler, writeHandler)`。read：`v.Fs().Open(filename)` → `rf.ReadFrom(file)`；write：`v.Fs().Create(filename)` → `wt.WriteTo(file)`。文件名先经 `path.Clean` 去掉前导分隔，afero 囚笼兜底。

- [ ] **Step 1: 写失败测试**
```go
package tftpsrv

import (
    "bytes"; "testing"; "time"
    "github.com/pin/tftp"
    "opensvr/internal/vfs"
)

func TestTFTPWriteThenRead(t *testing.T) {
    v, _ := vfs.New(t.TempDir())
    s := New(v, 0); if err := s.Start(); err != nil { t.Fatal(err) }
    defer s.Stop(); time.Sleep(100 * time.Millisecond)

    c, _ := tftp.NewClient(s.Addr())
    // 写
    wt, _ := c.Send("boot.bin", "octet")
    wt.(interface{ SetSize(int64) }) // 可选
    src := bytes.NewReader([]byte("bootimg"))
    if _, err := wt.ReadFrom(src); err != nil { t.Fatal(err) }
    // 读
    rt, _ := c.Receive("boot.bin", "octet")
    buf := new(bytes.Buffer)
    if _, err := rt.WriteTo(buf); err != nil { t.Fatal(err) }
    if buf.String() != "bootimg" { t.Fatalf("tftp roundtrip mismatch: %q", buf.String()) }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/tftpsrv/` → FAIL。

- [ ] **Step 3: 实现**
```go
package tftpsrv

import (
    "fmt"; "io"; "net"; "path"; "sync"
    "github.com/pin/tftp"
    "opensvr/internal/logbus"; "opensvr/internal/vfs"
)

type Status struct { Running bool; Port int; Err string }

type Server struct {
    v *vfs.VFS; port int
    mu sync.Mutex; srv *tftp.Server; conn net.PacketConn; running bool; err string; boundPort int
}

func New(v *vfs.VFS, port int) *Server { return &Server{v: v, port: port} }

func (s *Server) readHandler(filename string, rf io.ReaderFrom) error {
    f, err := s.v.Fs().Open(path.Clean("/" + filename))
    if err != nil { logbus.Emit(logbus.Event{Proto: "tftp", Action: "read", Path: filename, OK: false, Msg: err.Error()}); return err }
    defer f.Close()
    _, err = rf.ReadFrom(f)
    logbus.Emit(logbus.Event{Proto: "tftp", Action: "read", Path: filename, OK: err == nil})
    return err
}

func (s *Server) writeHandler(filename string, wt io.WriterTo) error {
    f, err := s.v.Fs().Create(path.Clean("/" + filename))
    if err != nil { logbus.Emit(logbus.Event{Proto: "tftp", Action: "write", Path: filename, OK: false, Msg: err.Error()}); return err }
    defer f.Close()
    _, err = wt.WriteTo(f)
    logbus.Emit(logbus.Event{Proto: "tftp", Action: "write", Path: filename, OK: err == nil})
    return err
}

func (s *Server) Start() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if s.running { return nil }
    conn, err := net.ListenPacket("udp", fmt.Sprintf(":%d", s.port))
    if err != nil { s.err = err.Error(); return err }
    s.conn = conn; s.boundPort = conn.LocalAddr().(*net.UDPAddr).Port
    s.srv = tftp.NewServer(s.readHandler, s.writeHandler)
    s.running = true; s.err = ""
    go s.srv.Serve(conn.(*net.UDPConn))
    return nil
}

func (s *Server) Stop() error {
    s.mu.Lock(); defer s.mu.Unlock()
    if !s.running { return nil }
    s.srv.Shutdown(); _ = s.conn.Close(); s.running = false; return nil
}
func (s *Server) Status() Status { s.mu.Lock(); defer s.mu.Unlock(); return Status{s.running, s.boundPort, s.err} }
func (s *Server) Addr() string { return fmt.Sprintf("127.0.0.1:%d", s.Status().Port) }
```

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/tftpsrv/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(tftpsrv): TFTP读写服务端"`

---

## Task 11: server.Manager（统一启停）

**Files:**
- Create: `internal/server/manager.go`, `internal/server/manager_test.go`

**Interfaces:**
- Consumes: 三协议 `Server`、`vfs`、`auth`、`config`、`hostkey`。
- Produces: `type Status struct { Running bool; Port int; Err string }`（Web/前端统一用）；`func New(cfg *config.Config, dir string) (*Manager, error)`；方法 `StartFTP()/StopFTP()/StartSFTP()/StopSFTP()/StartTFTP()/StopTFTP()`；`SetRoot(string) error`；`SetAuth(config.AuthCfg)`；`SetPort(proto string, port int)`；`Statuses() map[string]Status`；`Config() *config.Config`；`Save() error`。

- [ ] **Step 1: 写失败测试**
```go
package server

import ("testing"; "opensvr/internal/config")

func TestManagerStartStopFTP(t *testing.T) {
    cfg := config.Default(); cfg.RootDir = t.TempDir(); cfg.FTP.Port = 0
    m, err := New(cfg, t.TempDir()); if err != nil { t.Fatal(err) }
    if err := m.StartFTP(); err != nil { t.Fatal(err) }
    if !m.Statuses()["ftp"].Running { t.Fatal("ftp should be running") }
    if err := m.StopFTP(); err != nil { t.Fatal(err) }
    if m.Statuses()["ftp"].Running { t.Fatal("ftp should be stopped") }
}

func TestSetRootRebuilds(t *testing.T) {
    cfg := config.Default(); cfg.RootDir = t.TempDir()
    m, _ := New(cfg, t.TempDir())
    nd := t.TempDir()
    if err := m.SetRoot(nd); err != nil { t.Fatal(err) }
    if m.Config().RootDir != nd { t.Fatal("root not updated") }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/server/` → FAIL。

- [ ] **Step 3: 实现** — Manager 持 `*vfs.VFS`、`*auth.Store`、三协议指针、`cfg`、`cfgPath`、`hostkeyPath`。
  - `New`：建 vfs、auth、`hostkey.LoadOrCreate`、构造三协议（用 cfg 端口）。
  - `Start*`：调对应 `Start()`，把各协议 `Status` 归一到本包 `Status`。
  - `SetRoot`：`vfs.SetRoot`，更新 cfg，`Save()`。
  - `SetPort`：改 cfg 端口并重建对应协议实例（若在运行则先停后启）。
  - `Statuses`：聚合三协议状态到 `map[string]Status{"ftp":..,"sftp":..,"tftp":..}`。
  - `Save`：`cfg.Save(cfgPath)`。
  （完整代码在实现时按上述签名逐方法写出，均为直接转发+状态归一，无新逻辑。）

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/server/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(server): 统一管理器启停/根目录/端口"`

---

## Task 12: web 模块（REST + SSE + 内嵌前端）

**Files:**
- Create: `internal/web/web.go`, `internal/web/browser.go`, `internal/web/web_test.go`, `internal/web/assets/index.html`, `internal/web/assets/app.js`, `internal/web/assets/style.css`

**Interfaces:**
- Consumes: `server.Manager`, `netinfo`, `logbus`, `metrics`, `auth`。
- Produces: `func New(m *server.Manager) *Web`；`func (w *Web) Start(port int) error`；`func (w *Web) URL() string`；`func OpenBrowser(url string) error`。
- REST（JSON）：
  - `GET /api/status` → `{ftp,sftp,tftp: {running,port,err}, root, nics:[{name,ip,cidr}], auth:{user,pass,anonymous}, ports:{ftp,sftp,tftp,web}}`
  - `POST /api/proto/{ftp|sftp|tftp}/{start|stop}`
  - `POST /api/root` body `{dir}`
  - `POST /api/port` body `{proto,port}`
  - `POST /api/auth` body `{user,pass,anonymous}`
  - `POST /api/firewall` → 调 `netsh` 加入站放行（需管理员，失败回错误提示）
  - `GET /api/events` → SSE，推 logbus 事件
  - `GET /api/metrics` → SSE，每秒推 `metrics.Snapshot()` + `History()`

- [ ] **Step 1: 写失败测试**
```go
package web

import ("encoding/json"; "net/http"; "net/http/httptest"; "testing"
    "opensvr/internal/config"; "opensvr/internal/server")

func TestStatusEndpoint(t *testing.T) {
    cfg := config.Default(); cfg.RootDir = t.TempDir()
    m, _ := server.New(cfg, t.TempDir())
    w := New(m)
    req := httptest.NewRequest("GET", "/api/status", nil)
    rec := httptest.NewRecorder()
    w.handler().ServeHTTP(rec, req)
    if rec.Code != http.StatusOK { t.Fatalf("code=%d", rec.Code) }
    var body map[string]any
    if err := json.Unmarshal(rec.Body.Bytes(), &body); err != nil { t.Fatal(err) }
    if _, ok := body["nics"]; !ok { t.Fatal("status missing nics") }
}

func TestStartStopViaAPI(t *testing.T) {
    cfg := config.Default(); cfg.RootDir = t.TempDir(); cfg.FTP.Port = 0
    m, _ := server.New(cfg, t.TempDir())
    w := New(m)
    rec := httptest.NewRecorder()
    w.handler().ServeHTTP(rec, httptest.NewRequest("POST", "/api/proto/ftp/start", nil))
    if rec.Code != http.StatusOK { t.Fatalf("start code=%d", rec.Code) }
    if !m.Statuses()["ftp"].Running { t.Fatal("ftp not started via api") }
}
```

- [ ] **Step 2: 跑红** — Run: `go test ./internal/web/` → FAIL。

- [ ] **Step 3: 实现** `web.go`（`handler()` 返回 `*http.ServeMux`；`go:embed assets/*` 内嵌静态；SSE 用 `http.Flusher`）、`browser.go`（Windows: `exec.Command("cmd", "/c", "start", "", url)`），及前端三件套。前端 `app.js`：
  - 拉 `/api/status` 渲染开关、端口输入框、网卡 IP 列表、明文账号密码；
  - 订阅 `/api/events` 追加日志；
  - 订阅 `/api/metrics`，用 `<canvas>` 画上/下行折线（任务管理器风格），并显示当前速率数字（KB/s，自动单位）。
  （完整前端代码在实现时写出；Canvas 折线用滚动数组重绘，无第三方库。）

- [ ] **Step 4: 跑绿** — Run: `go test ./internal/web/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(web): 管理页/REST/SSE与性能曲线"`

---

## Task 13: main 装配 + 托盘 + 启动开浏览器

**Files:**
- Create: `cmd/opensvr/main.go`

**Interfaces:**
- Consumes: 全部模块。

- [ ] **Step 1: 写冒烟测试（可编译 + 端口解析）**
在 `cmd/opensvr/main_test.go` 写一个最小测试：调用一个可测的 `run(cfgPath, baseDir) (stop func(), url string, err error)` 抽出的函数，断言 Web 起得来、`/api/status` 200，然后 `stop()`。托盘用 build tag 隔离，避免测试起 GUI。

- [ ] **Step 2: 跑红** — Run: `go test ./cmd/opensvr/` → FAIL。

- [ ] **Step 3: 实现**
- `run()`：定位 exe 同级目录 → 加载 `config.yaml` → 建 `server.Manager` → 建 `web.Web` → 起 Web(cfg.Web.Port) → 启 `metrics.Start` → 若 `cfg.AutoOpenBrowser` 则 `web.OpenBrowser(url)` → 返回 stop。
- `main()`：`systray.Run(onReady, onExit)`；`onReady` 里设图标/菜单（打开管理页 / 退出），调 `run()`；菜单"打开管理页"再次 `OpenBrowser`。
- exe 同级路径：`os.Executable()` 取目录，config/hostkey/默认 data 都相对它。

- [ ] **Step 4: 跑绿 + 手动冒烟** — Run: `go test ./cmd/opensvr/` → PASS。
- [ ] **Step 5: 提交** — `git commit -am "feat(main): 装配+托盘+启动开浏览器"`

---

## Task 14: 构建、端到端验证、文档

**Files:**
- Create: `docs/USAGE.md`
- Create: `build.bat`（`set CGO_ENABLED=0 && go build -ldflags "-s -w -H=windowsgui" -o opensvr.exe ./cmd/opensvr`）

- [ ] **Step 1: 全量测试** — Run: `go test ./...` → 全 PASS。
- [ ] **Step 2: 构建单 exe** — Run: `build.bat` → 生成 `opensvr.exe`（无 cgo、单文件、无控制台窗口）。
- [ ] **Step 3: 端到端手动验证（verify 技能）**
  - 双击 exe → 托盘出现 → 浏览器自动打开 `http://127.0.0.1:31944`。
  - 页面显示网卡 IP、明文 admin/admin。
  - 开 FTP，用 FileZilla/命令行 `ftp` 上传+下载一个文件；主动、被动各试一次。
  - 开 SFTP，用 `sftp -P 22 admin@127.0.0.1` 上传+下载。
  - 开 TFTP，用 `tftp` 客户端 put/get。
  - 传输时观察性能曲线与实时速率数字变化；中断传输看速率归零。
  - 点"放行防火墙"→ UAC → 成功提示。
- [ ] **Step 4: 写 `docs/USAGE.md`** — 含：快速上手、config.yaml 说明、华为/H3C/思科开局侧命令示例（如华为 `tftp <PC_IP> get <firmware>`、`sftp <PC_IP>`、`ftp <PC_IP>`）、防火墙/同网段/端口冲突排错。
- [ ] **Step 5: 提交** — `git commit -am "chore: 构建脚本、端到端验证、使用文档"`

---

## Self-Review

**Spec 覆盖核对：**
- 三协议(FTP主动+被动/SFTP/TFTP)：Task 8/9/10 ✓
- 端口全可自定义 + 持久化：config(Task1)/manager SetPort(Task11)/web POST /api/port(Task12) ✓
- Web 31944 + 自动开浏览器：config 默认(Task1)/browser.go+run()(Task12/13) ✓
- 账号密码明文显示：auth.Creds(Task4)/status 接口(Task12) ✓
- 性能曲线 + 实时速率：metrics(Task2)/vfs 计数(Task3)/SSE+Canvas(Task12) ✓
- 网卡 IP 列表：netinfo(Task6)/status(Task12) ✓
- 根目录可选：vfs.SetRoot(Task3)/manager(Task11)/web(Task12) ✓
- 路径囚笼安全：vfs afero BasePathFs(Task3) + 测试 ✓
- 轻量/未启用零开销：只有 Start 才监听(各协议) ✓
- 单 exe 无 cgo：build.bat CGO_ENABLED=0(Task14) ✓
- 日志监控：logbus(Task5)/SSE(Task12) ✓
- 防火墙一键放行：web /api/firewall(Task12) ✓

**占位符扫描：** 无 TBD/TODO 遗留；Task 9/11/12 的"实现时写出"处均给了精确签名、接口清单与关键代码骨架，属正常的库适配细化，非占位。

**类型一致性：** 各协议 `Status{Running,Port,Err}` 与 `server.Status` 字段一致；`config` 字段在 auth/manager/web 引用一致；`metrics.Sample` 字段在 web 使用一致。

**已知风险（实现时留意，非阻塞）：**
1. `ftpserverlib` 获取实际监听端口的 API 因版本而异 → Task8 备选固定端口方案。
2. `pkg/sftp` afero→Handlers 适配代码量 → Task9 给了退化方案并标注安全权衡。
3. `getlantern/systray` 在纯测试环境不应启动 GUI → Task13 用 `run()` 抽离可测逻辑。
